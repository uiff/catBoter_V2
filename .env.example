# CatBoter V3 - Umgebungsvariablen
# Kopiere diese Datei zu .env und passe die Werte an

# Backend Konfiguration
FLASK_ENV=production
FLASK_APP=main.py
FLASK_DEBUG=0

# Datenbank / Storage
DATA_DIR=/app/data
FEEDING_PLAN_DIR=/app/feedingPlan

# Logging
LOG_LEVEL=INFO
LOG_FILE=/app/logs/catboter.log

# Hardware Konfiguration (Raspberry Pi)
GPIO_ENABLED=true
I2C_ENABLED=true

# Netzwerk
BACKEND_PORT=5000
FRONTEND_PORT=80

# Security
# WICHTIG: Generiere eigene Werte mit: openssl rand -hex 32
SECRET_KEY=4d7548cf50ff31ba20dd6680d113a5107d90f732721d491e68f27439fa9820f2
API_KEY=generate-your-own-key-here

# WiFi Fallback (wird in backend/data/wifi_fallback_config.json gespeichert)
WIFI_FALLBACK_PASSWORD=ojoO9TkbVBSmupSCY3KFP751

# Optional: InfluxDB (falls verwendet)
INFLUX_ENABLED=false
INFLUX_URL=http://localhost:8086
INFLUX_TOKEN=your-token
INFLUX_ORG=catboter
INFLUX_BUCKET=sensors
